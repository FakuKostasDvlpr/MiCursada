# graphify
- **graphify** (`.claude/skills/graphify/SKILL.md`) - any input to knowledge graph. Trigger: `/graphify`
When the user types `/graphify`, use the installed graphify skill or instructions before doing anything else.

# portar-diseno
- **portar-diseno** (`.claude/skills/portar-diseno/SKILL.md`) — llevar la app al handoff de diseño con SDD y verificación en navegador. Trigger: `/portar-diseno`
Usala cuando haya un paquete de diseño nuevo en `design_handoff_mi_cursada*/`, cuando haya que corregir la fidelidad de una pantalla, o cuando el usuario diga que algo "no se ve como el handoff". **La fuente de verdad es `Mi Cursada.dc.html`, no el README.**
